<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/mvc"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xmlns:beans="http://www.springframework.org/schema/beans"
             xmlns:context="http://www.springframework.org/schema/context"
             xmlns:mongo="http://www.springframework.org/schema/data/mongo"
             xsi:schemaLocation="http://www.springframework.org/schema/mvc http://www.springframework.org/schema/mvc/spring-mvc.xsd
        http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd
        http://www.springframework.org/schema/data/mongo
          http://www.springframework.org/schema/data/mongo/spring-mongo-1.1.xsd">

    <!-- DispatcherServlet Context: defines this servlet's request-processing infrastructure -->

    <!-- Enables the Spring MVC @Controller programming model -->
    <annotation-driven/>
    <context:component-scan base-package="com.flynava.jupiter" scoped-proxy="targetClass"/>  
    <context:component-scan base-package="com.flynava.jupiter.security" scoped-proxy="targetClass"/> 
    
    
    
    <beans:bean id="model" class="org.springframework.web.servlet.ModelAndView" scope="prototype"/>

    <!-- Handles HTTP GET requests for /resources/** by efficiently serving up static resources in the ${webappRoot}/resources directory -->
    <resources mapping="/resources/**" location="/resources/"/>

    <!-- Resolves views selected for rendering by @Controllers to .jsp resources in the /WEB-INF/views directory -->
    <beans:bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <beans:property name="prefix" value="/Pages/"/>
        <beans:property name="suffix" value=".jsp"/>
    </beans:bean>
    
    
    <context:component-scan base-package="com.flynava.jupiter.security" scoped-proxy="targetClass"/>
    <mongo:repositories base-package="com.flynava.jupiter.daoInterface" />

    <mongo:db-factory id="mongoDbFactory" host="${mongoDbFactory.Ip}" port="${mongoDbFactory.Port}" dbname="${mongoDbFactory.dbName}" username="${mongoDbFactory.username}" password="${mongoDbFactory.password}"/>
   
	<beans:bean id="mMongoTemplateDB" class="org.springframework.data.mongodb.core.MongoTemplate" scope="singleton">
	    <beans:constructor-arg name="mongoDbFactory" ref="mongoDbFactory" />
	</beans:bean>

	
	<beans:bean class="org.springframework.beans.factory.config.PropertyPlaceholderConfigurer">
	<beans:property name="locations" value="/WEB-INF/spring/jupiterPlaceHolders.properties" />
	</beans:bean>
	


</beans:beans>